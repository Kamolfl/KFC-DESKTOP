import 'package:flutter/material.dart';

class ComboSecondScreen extends StatelessWidget {
  const ComboSecondScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('КОМБО ОБЕДЫ'),
      ),
    );
  }
}
